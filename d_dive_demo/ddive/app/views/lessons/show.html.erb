<h1>授業詳細</h1>
<table>
  <tr>
    <th>講義名</th>
    <th>教授</th>
    <th>学期</th>
    <th>校地</th>
    <th>時間</th>
    <th>教室</th>
  </tr>
  <tr>
    <td><%= link_to @lesson.title, @lesson.url %></td>
    <td><%= @lesson.teacher %></td>
    <td><%= @lesson.season  %></td>
    <td><%= @lesson.place  %></td>
    <td><%= @lesson.time  %></td>
    <td><%= @lesson.room %></td>
  </tr>
</table>
<p>
<%= link_to "時間・教室の編集", edit_lesson_path(@lesson.id) %>
</p>

<ul>
  <% if @lesson.notes.any? %>
    <%= @lesson.notes.each do |note|  %>
      <li><%= note.filename %></li>
    <% end %>
  <% else %>
    <p>講義ノートはアップロードされていません</p>
  <% end %>
      <li>
    <%= form_for [@lesson, @lesson.notes.build] do |f| %>
      <%= f.file_field :file %>
      <%= f.submit %>
    <% end %>
  </li>
</ul>
